version: 2

models:
  - name: dim_employee
    columns:
      - name: employee_key
        tests:
          - dbt_constraints.primary_key

  - name: dim_supplier
    columns:
      - name: supplier_key
        tests:
          - dbt_constraints.primary_key
